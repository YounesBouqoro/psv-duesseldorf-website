{
  "name": "psv-duesseldorf-website",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "check": "astro check",
    "check:content": "node scripts/check-content.mjs",
    "check:links": "node scripts/check-build-links.mjs",
    "build": "astro build",
    "preview": "astro preview",
    "verify": "npm run check && npm run check:content && npm run build && npm run check:links"
  },
  "dependencies": {
    "astro": "6.3.8"
  },
  "devDependencies": {
    "@astrojs/check": "0.9.9",
    "typescript": "6.0.3"
  }
}
