---
import Layout from "../components/layout/Layout.astro";

const facts = [
  "Mehrspartenverein in Düsseldorf",
  "Sportangebote für unterschiedliche Altersgruppen",
  "Vereinsleben mit Abteilungen, Kursen und Partnern",
  "Relaunch mit Fokus auf klare Informationen und bessere Auffindbarkeit"
];

const priorities = [
  {
    title: "Sportarten sichtbar machen",
    text: "Alle Abteilungen werden sauber strukturiert und mit Trainingszeiten, Kontakten und Hinweisen gepflegt."
  },
  {
    title: "Mitgliedschaft vereinfachen",
    text: "Interessierte sollen schneller verstehen, wie Probetraining, Beitritt und Downloads funktionieren."
  },
  {
    title: "Vertrauen stärken",
    text: "Kinderschutz, Ansprechpartner, Vereinsdaten und rechtliche Informationen werden klarer dargestellt."
  }
];
---

<Layout title="Verein">
  <section class="section page-hero">
    <div class="container hero-grid">
      <div>
        <p class="eyebrow">Der Verein</p>
        <h1>Polizei-Sport-Verein Düsseldorf e.V.</h1>
        <p class="intro">
          Der PSV Düsseldorf ist ein Mehrspartenverein mit vielfältigen Sportangeboten.
          Die neue Website bündelt Vereinsinformationen, Sportarten, Mitgliedschaft und Kontaktwege in einer klaren Struktur.
        </p>
      </div>

      <aside class="status-card">
        <p class="status-label">Hinweis</p>
        <h2>Vereinsdaten prüfen</h2>
        <p>
          Mitgliedszahlen, Adressen, Ansprechpartner und rechtliche Angaben werden vor dem finalen Launch redaktionell verifiziert.
        </p>
      </aside>
    </div>
  </section>

  <section class="section">
    <div class="container fact-grid">
      {facts.map((fact) => (
        <div class="fact-card">{fact}</div>
      ))}
    </div>
  </section>

  <section class="section light-section">
    <div class="container">
      <p class="eyebrow">Relaunch-Fokus</p>
      <h2>Was die neue Website leisten soll</h2>

      <div class="priority-grid">
        {priorities.map((item) => (
          <article class="priority-card">
            <h3>{item.title}</h3>
            <p>{item.text}</p>
          </article>
        ))}
      </div>
    </div>
  </section>
</Layout>

<style>
  .page-hero {
    background:
      radial-gradient(circle at top right, rgba(127, 194, 65, 0.18), transparent 34%),
      linear-gradient(135deg, #ffffff 0%, #f4f8f1 100%);
  }

  .hero-grid {
    display: grid;
    grid-template-columns: minmax(0, 1fr) 390px;
    gap: 44px;
    align-items: start;
  }

  .eyebrow,
  .status-label {
    color: var(--accent-strong);
    font-weight: 800;
    text-transform: uppercase;
    letter-spacing: 0.08em;
  }

  h1 {
    max-width: 980px;
    font-size: clamp(42px, 7vw, 78px);
    line-height: 0.98;
    margin: 16px 0 24px;
  }

  .intro {
    max-width: 760px;
    color: var(--muted);
    font-size: 1.15rem;
  }

  .status-card,
  .fact-card,
  .priority-card {
    padding: 28px;
    border: 1px solid var(--border);
    border-radius: 28px;
    background: #ffffff;
    box-shadow: 0 16px 50px rgba(0,0,0,0.06);
  }

  .status-card p,
  .priority-card p {
    color: var(--muted);
  }

  .fact-grid {
    display: grid;
    grid-template-columns: repeat(4, minmax(0, 1fr));
    gap: 18px;
  }

  .fact-card {
    color: var(--accent-strong);
    font-weight: 900;
  }

  .light-section {
    background: var(--surface);
  }

  .priority-grid {
    display: grid;
    grid-template-columns: repeat(3, minmax(0, 1fr));
    gap: 20px;
    margin-top: 28px;
  }

  @media (max-width: 980px) {
    .hero-grid,
    .priority-grid {
      grid-template-columns: 1fr;
    }

    .fact-grid {
      grid-template-columns: repeat(2, minmax(0, 1fr));
    }
  }

  @media (max-width: 560px) {
    .fact-grid {
      grid-template-columns: 1fr;
    }
  }
</style>
